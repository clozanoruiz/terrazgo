{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the main window. App-defined commands registered via generate_handler! are not ACL-gated; core:default covers the events/window plumbing the injected global API bundle relies on.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-save", "dialog:allow-open", "dialog:allow-message"]
}
