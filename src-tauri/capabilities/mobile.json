{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Mobile-only additions to the baseline capability: the geolocation plugin behind the map's GPS point lookup (P5). Desktop builds skip this file via the platforms gate — the plugin does not exist there.",
  "windows": ["main"],
  "platforms": ["android", "iOS"],
  "permissions": [
    "geolocation:allow-check-permissions",
    "geolocation:allow-request-permissions",
    "geolocation:allow-get-current-position",
    "geolocation:allow-watch-position",
    "geolocation:allow-clear-watch"
  ]
}
