# Build output and vendored/generated trees; Rust is cargo fmt's job.
dist/
target/
src-tauri/gen/
Cargo.lock
package-lock.json
*.md
# Generated by scripts/gen-third-party.mjs. JSON.stringify cannot produce what
# prettier wants at any indent — prettier collapses short arrays onto one line —
# so the choice is to ignore it or to reformat every regeneration.
src/lib/thirdPartyLicences.json
# Harvested service responses used as test fixtures — byte-exact, never reformatted.
crates/*/tests/fixtures/
crates/module-cue/src/demo/*.geojson
# Vendored official reference documents (FEGA schemas) — byte-exact, never reformatted.
docs/references/
